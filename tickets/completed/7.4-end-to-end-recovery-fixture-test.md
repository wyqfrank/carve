# Ticket 7.4 -- End-to-End Recovery Fixture Test

**Epic:** 7 -- Testing
**Complexity:** High

## Fixtures Created

- `fixture_valid_jpeg()` — SOI + APP0 + DQT + SOF0(320×240) + DHT + SOS + entropy + EOI
- `fixture_progressive_jpeg()` — SOI + DQT + SOF2(480×320) + SOS + entropy + EOI
- `fixture_truncated_jpeg()` — SOI + DQT + SOF0(160×120) + SOS + entropy (no EOI)
- `fixture_noise_with_embedded_soi()` — 100 zero bytes with SOI at offset 50
- `fixture_corrupt_seg_len()` — SOI + APP0 with length field overflowing EOF
- Overlapping candidates: constructed directly as `ValidatedCandidate` values

## Tests

- [x] Valid JPEG recovered successfully (`valid_jpeg_fixture_recovers_complete_candidate`, `valid_jpeg_fixture_confidence_score`)
- [x] Progressive JPEG metadata parsed correctly (`progressive_jpeg_fixture_metadata_parsed_correctly`, `progressive_jpeg_recovered_via_full_pipeline`)
- [x] Truncated JPEG handled according to policy (`truncated_jpeg_fixture_emits_truncated_candidate`, `truncated_jpeg_fixture_eoi_patch_flag`)
- [x] Noise candidates rejected (`noise_with_embedded_soi_yields_no_candidate`, `noise_with_embedded_soi_full_pipeline_yields_no_candidates`)
- [x] Overlapping candidates suppressed correctly (`overlapping_candidates_suppressed_correctly`, `two_concatenated_jpegs_both_recovered`)

11 tests total, all passing.
