# Ticket 2.8 — Add Restart Marker Detection and Logging

**Epic:** 2 — Camera-Aware JPEG Reconstruction
**Complexity:** Medium

## Goal

Inspect carved entropy streams for restart marker usage and log whether Canon IXUS 310 HS images appear to use restart intervals.

## Why

Before implementing full restart-aware repair, you should confirm whether these files actually contain restart markers and how often they appear.

## Scope

Implement logic to scan entropy data for:

- `FF D0` to `FF D7`
- spacing between restart markers
- whether spacing appears regular

Log findings per carved file.

Do not do full decoder resync yet; this ticket is detection only.

## Deliverables

- Restart marker scanner
- Per-file restart marker summary
- Note on whether Canon IXUS 310 HS profile should include DRI

## Acceptance Criteria

- [ ] Scanner detects restart markers if present
- [ ] Output includes marker count and approximate spacing
- [ ] Enough evidence is gathered to justify or skip full restart-aware repair
