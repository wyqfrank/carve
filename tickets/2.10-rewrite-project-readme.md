# Ticket 2.10 — Rewrite Project README

**Epic:** 2 — Camera-Aware JPEG Reconstruction
**Complexity:** Low–Medium

## Goal

Rewrite the project README to clearly document the purpose, architecture, and recovery pipeline of the project, including the transition from basic JPEG carving to camera-aware reconstruction for the Canon IXUS 310 HS.

The README should explain both the technical challenge and the engineering approach so that someone unfamiliar with the project can understand it quickly.

## Why

The project has evolved significantly beyond a basic file carver:

- **Phase 1:** entropy-aware JPEG carving, byte stuffing handling, restart marker tolerance
- **Phase 2:** camera-specific header reconstruction, entropy offset search, JPEG recovery improvements

This is far more interesting than a typical file carver, but the README must explain it clearly.

## Scope

Rewrite the root `README.md` to include the following sections.

### 1. Project Overview

High-level explanation of the project. Example concept:

> Carve is a digital forensics tool written in Rust that recovers corrupted JPEG images from raw byte streams. Unlike traditional file carving tools that rely solely on marker detection, Carve performs entropy-aware JPEG scanning and camera-specific reconstruction to improve recovery quality.

Explain:

- motivation
- problem space
- what makes the approach different

### 2. Example Results

Add before / after recovery examples showing:

```
Raw carve
    ↓
Camera-aware reconstruction
    ↓
Recovered image
```

Include a few of the corrupted images already produced. This visually proves the project works.

### 3. How JPEG Recovery Works

Brief explanation of JPEG structure:

```
SOI → APP segments → DQT → SOF → DHT → SOS → Entropy stream → EOI
```

Explain why carving JPEGs is hard: entropy streams, byte stuffing, restart markers. Keep it concise but technical.

### 4. Architecture

Explain project modules with an ASCII layout. Example:

```
src/
  carver/
    jpeg_scanner.rs
    entropy_scan.rs
  jpeg/
    marker_parser.rs
  reconstruct/
    camera_profile.rs
    header_builder.rs
    entropy_offset.rs
```

Explain what each layer does.

### 5. Recovery Pipeline

Document the full pipeline with an ASCII diagram:

```
Disk Image / Raw Byte Stream
        ↓
JPEG Marker Detection
        ↓
Entropy Stream Extraction
        ↓
Camera-Specific Header Reconstruction
        ↓
Entropy Offset Search
        ↓
Candidate Ranking
        ↓
Recovered JPEG Output
```

### 6. Camera-Specific Recovery

Explain the Canon IXUS 310 HS aspect:

- quantisation tables extracted from reference images
- Huffman tables reused
- SOF template rebuilt with recovered dimensions
- header reconstruction improves colour correctness

This part makes the project stand out from generic carving tools.

### 7. Running the Tool

Include usage examples and example output directory.

### 8. Limitations

Document honest current limitations:

- partial entropy corruption
- overwritten data
- unknown dimensions
- extreme fragmentation

### 9. Future Work

Mention potential expansions:

- restart-marker resynchronisation
- AI artefact repair
- multi-camera profiles
- progressive JPEG support
- entropy scoring improvements

## Deliverables

- Fully rewritten `README.md`
- Architecture diagram (ASCII)
- Example recovered images
- Clear explanation of Phase 1 vs Phase 2 pipeline

## Acceptance Criteria

- [ ] README clearly explains the problem and solution
- [ ] Includes a recovery pipeline diagram
- [ ] Includes example results
- [ ] Explains the Canon IXUS 310 HS reconstruction strategy
- [ ] Project is understandable in under 2 minutes of reading

## Optional

Add `docs/jpeg_structure.md` for deeper technical explanation if needed, keeping the README readable while still documenting the project thoroughly.
