# Ticket 1.2 -- Implement Marker Constants + Helpers

**Epic:** 1 -- JPEG Marker Infrastructure
**Complexity:** Low

## Goal

In `markers.rs`:

- Define constants:
  - SOI, EOI, SOS
  - SOF0, SOF2
  - DQT, DHT
  - APP0--APP15
  - COM
  - RST0--RST7
- Implement helpers:

```rust
fn is_restart_marker(byte: u8) -> bool
fn has_length(marker: u8) -> bool
```

## Definition of Done

- [ ] Unit tests verifying marker classification