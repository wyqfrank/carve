# Ticket 4.1 -- Implement Truncated Policy

**Epic:** 4 -- Truncated Recovery
**Complexity:** Medium

## Goal

Modify `validate.rs`:

If:

- Header parsing succeeded
- SOS reached
- EOI not found
- and `allow_truncated == true`

Then:

- End at entropy boundary or max_size
- Set:
  - status = Truncated
  - patched_eoi = false (for now)

## Definition of Done

- [ ] Truncated JPEG emitted
- [ ] Correct metadata included
- [ ] Status properly labeled
