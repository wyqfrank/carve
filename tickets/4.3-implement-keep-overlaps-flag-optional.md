# Ticket 4.3 -- Implement --keep-overlaps Flag (Optional)

**Epic:** 4 -- Truncated Recovery
**Complexity:** Low

## Goal

Allow user to bypass overlap suppression.

If:

- `--keep-overlaps == true`

Then:

- Emit all validated candidates
- Do not suppress overlapping ranges

Default behavior remains suppression enabled.

## Definition of Done

- [ ] Flag parsed correctly
- [ ] Default mode suppresses overlaps
- [ ] Flag mode emits all candidates
